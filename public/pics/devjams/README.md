# DevJams'24 photo

Drop **1 picture** in this folder, named exactly:

- `01.jpg`

JPG / PNG / WebP all work — just keep the filename + extension matching.
If you change the extension, update the `src` in `src/pages/index.astro` (search for `/pics/devjams/`).
