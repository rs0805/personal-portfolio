# ACM-W VIT photos

Drop **3 pictures** in this folder, named exactly:

- `01.jpg`
- `02.jpg`
- `03.jpg`

JPG / PNG / WebP all work — just keep the filename + extension matching what the page expects.
If you change extensions, update the `src` values in `src/pages/index.astro` (search for `/pics/acmw/`).
