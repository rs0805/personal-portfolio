<div class="hero-art">
  <div class="photo-grid">

    <div class="frame frame-feature">
      <img src="/pics/01.jpeg" alt="A picture of me" />

      <div class="frame-overlay">
        <span class="corner tl">[</span>
        <span class="corner tr">]</span>
        <span class="corner bl">[</span>
        <span class="corner br">]</span>

        <span class="frame-num">01</span>
      </div>
    </div>

    <div class="frame">
      <img src="/pics/02.jpeg" alt="A picture of the world" />

      <div class="frame-overlay">
        <span class="corner tl">[</span>
        <span class="corner tr">]</span>
        <span class="corner bl">[</span>
        <span class="corner br">]</span>

        <span class="frame-num">02</span>
      </div>
    </div>

    <div class="frame">
      <img src="/pics/03.jpeg" alt="A picture of the world" />

      <div class="frame-overlay">
        <span class="corner tl">[</span>
        <span class="corner tr">]</span>
        <span class="corner bl">[</span>
        <span class="corner br">]</span>

        <span class="frame-num">03</span>
      </div>
    </div>

  </div>

  <div class="id-name">
    <div class="id-full">Harshitaa Kashyap</div>
    <div class="id-role">full-stack developer</div>
  </div>

  <div class="divider"></div>
</div>

<style>
  .hero-art {
    display: flex;
    flex-direction: column;
    gap: 16px;
    flex: 1;
  }

  .photo-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 6px;
  }

  .frame {
    position: relative;
    overflow: hidden;
    background: #060606;
    border-radius: 4px;
    aspect-ratio: 1 / 1;
    transition: transform 0.2s ease;
  }

  .frame:hover {
    transform: translateY(-2px);
  }

  .frame-feature {
    grid-column: 1 / -1;
    aspect-ratio: 16 / 10;
  }

  .frame img {
    width: 100%;
    height: 100%;
    display: block;
    object-fit: cover;
    filter: grayscale(10%);
  }

  .frame-overlay {
    position: absolute;
    inset: 0;
    pointer-events: none;
  }

  .corner {
    position: absolute;
    color: var(--accent);
    font-family: var(--font-mono);
    font-size: 16px;
    font-weight: 700;
    line-height: 1;
  }

  .corner.tl {
    top: 8px;
    left: 8px;
  }

  .corner.tr {
    top: 8px;
    right: 8px;
  }

  .corner.bl {
    bottom: 8px;
    left: 8px;
  }

  .corner.br {
    bottom: 8px;
    right: 8px;
  }

  .frame-num {
    position: absolute;
    top: 10px;
    right: 16px;
    color: var(--text-faint);
    font-size: 13px;
    font-weight: 700;
    letter-spacing: 0.25em;
  }

  .frame-feature .frame-num {
    font-size: 18px;
  }

  .id-name {
    text-align: center;
  }

  .id-full {
    font-size: 16px;
    font-weight: 700;
    color: var(--text);
  }

  .id-role {
    margin-top: 2px;
    font-size: 11px;
    color: var(--text-dim);
    letter-spacing: 0.04em;
  }

  .divider {
    margin-top: 4px;
    border-top: 1px dashed var(--border);
  }
</style>